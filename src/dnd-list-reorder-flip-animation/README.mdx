# dnd-list-reorder-flip-animation

- FLIPアニメーションを組み込んだもの。流れ的には以下のような処理を組み込む
  - まず、ステートをAからBに変化させる（ここで状態的にはBになっている）
  - ポジションをAにずらす（transition無しで0フレームで）
  - ポジションをBにずらす（transitionしながら）
- これを「入れ替わったカード達」に組み込むと、入れ替わられる要素達がスムーズに動く
